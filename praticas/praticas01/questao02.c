/* Faça um programa em C que calcule a área de um triângulo (a = base x altura / 2). Considere base e altura números inteiros maior que 0.
*/
#include <stdio.h> 
#include <math.h>

int main(){

  float b = 0;
  float h > 0;

  float area = b * h / 2;

  printf("A area do trinangulo e %f\n", area);

  return 0;
}